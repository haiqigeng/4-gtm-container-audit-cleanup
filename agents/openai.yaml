interface:
  display_name: "GTM Container Audit & Optimize"
  short_description: "Audit and optimize a GTM web container"
  default_prompt: "Use $gtm-container-audit-optimize to audit the GTM web-container export I provide in one comprehensive pass with two independent audits in parallel, reconcile their findings, statically validate the combined recommendations, and deliver one verified analyst workbook without changing GTM."

policy:
  allow_implicit_invocation: true
